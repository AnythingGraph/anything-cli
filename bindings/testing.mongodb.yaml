source_id: mongo_main

entities:
  transaction:
    from: transactions
    id: sku
    fields:
      user_id: userID
      currency: currency
      amount: amount
      payment_method: paymentMethod
      status: status
      sku: sku

relationships:
  user_transactions:
    object: transaction
    link_column: userID
