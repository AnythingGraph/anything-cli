source_id: warehouse_pg

entities:
  crm_user:
    from: users
    id: user_id
    fields:
      user_id: user_id
      full_name: full_name

relationships:
  linked_to_user:
    object: crm_user
    link_column: user_id
    subject_link_column: user_id
