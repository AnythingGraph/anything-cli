# Playbooks

Sample playbooks for AnythingGraph CLI. See the **[main README](../README.md)** for overview, MCP usage, and setup.

| Playbook | Sources |
|----------|---------|
| `simple-crm-access` | Postgres |
| `crm-payroll-access` | Postgres + CSV |

```bash
cargo run -p anythinggraph-ag -- validate --playbooks playbooks
```
